---
controller_inventories:
  - name: "{{ mgmt_inventory }}"
    description: "Mgmt Inventory"
    organization: "{{ organization }}"
    state: present
  - name: "{{ wkld_inventory }}"
    description: "Wkld Inventory"
    organization: "{{ organization }}"
    state: present
